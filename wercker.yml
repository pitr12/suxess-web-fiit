box: ruby
build:
  steps:
    - script:
      name: Build ready
      code: echo "Ready-to-roll"

deploy:
  steps:
    - script:
        name: install capistrano and capistrano-git-copy
        code: |
          gem install capistrano
          gem install capistrano-git-copy

    - script:
        name: execute capistrano deploy
        code: cap production deploy
